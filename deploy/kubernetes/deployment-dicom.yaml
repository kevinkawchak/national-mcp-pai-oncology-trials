apiVersion: apps/v1
kind: Deployment
metadata:
  name: trialmcp-dicom
  namespace: trialmcp
  labels:
    app: trialmcp-dicom
    app.kubernetes.io/part-of: national-mcp-pai-oncology-trials
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trialmcp-dicom
  template:
    metadata:
      labels:
        app: trialmcp-dicom
    spec:
      containers:
        - name: dicom
          image: trialmcp-dicom:0.7.0
          ports:
            - containerPort: 8003
          env:
            - name: TRIALMCP_STORAGE_BACKEND
              valueFrom:
                configMapKeyRef:
                  name: trialmcp-config
                  key: storage_backend
            - name: TRIALMCP_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: trialmcp-config
                  key: log_level
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: trialmcp-dicom
  namespace: trialmcp
spec:
  selector:
    app: trialmcp-dicom
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
