apiVersion: apps/v1
kind: Deployment
metadata:
  name: trialmcp-provenance
  namespace: trialmcp
  labels:
    app: trialmcp-provenance
    app.kubernetes.io/part-of: national-mcp-pai-oncology-trials
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trialmcp-provenance
  template:
    metadata:
      labels:
        app: trialmcp-provenance
    spec:
      containers:
        - name: provenance
          image: trialmcp-provenance:0.7.0
          ports:
            - containerPort: 8005
          env:
            - name: TRIALMCP_STORAGE_BACKEND
              valueFrom:
                configMapKeyRef:
                  name: trialmcp-config
                  key: storage_backend
            - name: TRIALMCP_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: trialmcp-config
                  key: log_level
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: trialmcp-provenance
  namespace: trialmcp
spec:
  selector:
    app: trialmcp-provenance
  ports:
    - port: 8005
      targetPort: 8005
  type: ClusterIP
